import math
#This function check if the pillars can be built above this coordinate
#radio is of arco bridge, height is the point in
def check_posible_bride(radio,height,x,y):
    height=math.sqrt(pow(r,2)-pow(x,2)
    if
    return 0
